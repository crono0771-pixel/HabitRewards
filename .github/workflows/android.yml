name: Build Android APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
        cache: 'gradle'

    - name: Grant execute permission for gradlew
      run: chmod +x ./HabitRewards/gradlew

    - name: Build APK (Debug)
      run: ./HabitRewards/gradlew assembleDebug --project-dir ./HabitRewards

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: HabitRewards-APK
        path: HabitRewards/app/build/outputs/apk/debug/app-debug.apk
